#include "bits/stdc++.h"

using namespace std;
using ll = long long;
const ll INF = 1e18;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    ll n1, n2;
    cin >> n1 >> n2;
    cout << abs(n1 - n2);

    return 0;
}

